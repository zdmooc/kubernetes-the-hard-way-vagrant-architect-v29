apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sample-pvc
  namespace: smoke
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard-local
